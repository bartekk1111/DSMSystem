spring:
  application:
    name: dsmsystem

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://db.ntefcmebdkhkdkhdchxx.supabase.co:5432/postgres?sslmode=require
    username: postgres
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect